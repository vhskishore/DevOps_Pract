name: Workflow 2

on:
  workflow_run:
    workflows: ["Workflow 1"]
    branches: [actions]
    types:
      - completed

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy application
        run: echo "Deploying application...."
